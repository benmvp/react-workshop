import React from 'react';

export default class EmailForm extends React.Component {
    render() {
        return (
            <div>
                <h2>Add a new email</h2>
            </div>
        );
    }
}
